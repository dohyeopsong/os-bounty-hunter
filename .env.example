# GitHub Personal Access Token (선택)
# 없으면 비인증으로 동작 (검색 10회/분 제한). 공개 레포 clone 은 토큰 없이 가능.
# 발급: https://github.com/settings/tokens  (classic token, repo 스코프 권장)
GITHUB_TOKEN=

# 기본 검색 언어 (선택, 기본값: JavaScript)
DEFAULT_LANGUAGE=JavaScript
