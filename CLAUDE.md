# OS-Bounty-Hunter — 에이전트 행동 지침 (Guardrails)

이 파일은 헤드리스 Claude(`claude-glm -p`)가 워크스페이스에서 Solve 단계를
수행할 때 자동 발견하는 지침이다. 아래 규칙은 절대적이다.

## 안전 (가장 중요)

1. **모든 git/파일 작업은 `workspace/` 디렉터리 내부에서만 수행한다.**
   프로젝트 루트나 그 상위 디렉터리의 파일을 수정하지 않는다.
2. **`git push` 와 모든 원격 조작을 절대 금지한다.** `git push`,
   `git remote`, `gh pr`, `gh repo` 등 원격에 영향을 주는 명령은 호출하지 않는다.
   PR 생성도 자동으로 하지 않는다.
3. 사용자가 지정한 이슈 번호의 레포(`workspace/` 하위)에서만 작업한다.

## 코드 수정 품질

4. **포맷팅(띄어쓰기, 줄바꿈, 따옴표 스타일)만을 변경하는 커밋/수정을 금지한다.**
   반드시 로직의 수정(조건문, 데이터 구조, API 응답 처리 등)이 포함되어야 한다.
5. 이슈 본문과 댓글의 요구사항을 먼저 파악한 뒤, 최소한의 변경으로 해결한다.
   관련 없는 파일을 함부로 수정하지 않는다.

## 테스트

6. 대상 프로젝트에 `npm test`(또는 `package.json` 의 `test` 스크립트)가
   존재하면 코드 수정 후 반드시 실행한다.
7. 테스트가 실패하면 원인을 분석하고, 가능하면 수정한다.
   어려우면 실패 원인을 결과에 명시한다. 테스트가 없으면 "N/A" 로 기록한다.

## 출력 형식 (반드시 준수)

최종 응답은 아래 마크다운 헤더 구조를 정확히 따른다. 다른 헤더를 추가하지 않는다.

```
## Summary
(무엇을 왜 수정했는지 논리적 근거)

## Changes
(기존 코드 vs 변경 코드의 핵심 차이점)

## Test Results
(npm test 결과. 없으면 "N/A", 실패 시 원인)

## PR Description
(GitHub PR 제출용 영문 본문. 복사해 바로 쓸 수 있게)
```
